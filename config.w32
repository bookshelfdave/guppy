// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("riak", "for riak support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("riak", "enable riak support", "no");

if (PHP_RIAK != "no") {
	EXTENSION("riak", "riak.c");
}

